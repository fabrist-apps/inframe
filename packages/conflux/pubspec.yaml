name: conflux
description: Pure functional values and effectful composition for Inframe.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  chrono_id:
    path: ../chrono_id
  context:
    path: ../context
  dart_mappable: ^4.10.0
  timezone: ^0.11.1

dev_dependencies:
  build_runner: ^2.16.1
  dart_mappable_builder: ^4.10.0
  test: ^1.26.3
