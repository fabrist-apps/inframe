name: inlet
description: In-process and HTTP routing with streams, server-sent events, and WebSockets.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.13.3 <4.0.0'

dependencies:
  context: any

dev_dependencies:
  test: ^1.26.3
