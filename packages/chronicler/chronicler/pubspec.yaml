name: chronicler
description: Structured telemetry capture and bounded delivery for Dart applications.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  ack: ^1.2.0
  ack_chrono_id: ^0.1.0
  chrono_id: ^0.1.0
  conflux: ^0.1.0
  context: ^0.1.0
  dart_mappable: ^4.10.0
  dart_mappable_conflux: ^0.1.0

dev_dependencies:
  build_runner: ^2.16.1
  dart_mappable_builder: ^4.10.0
  fake_async: ^1.3.3
  test: ^1.31.2
