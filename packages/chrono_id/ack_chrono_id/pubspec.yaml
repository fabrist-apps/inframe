name: ack_chrono_id
description: Chrono ID validation for Ack string schemas.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  ack: ^1.2.0
  chrono_id: ^0.1.0

dev_dependencies:
  test: ^1.26.3
