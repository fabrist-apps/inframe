name: chrono_id
description: Timestamp-bearing string IDs with optional prefixes.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  test: ^1.26.3
