name: clickhouse
description: Internal HTTP client for ClickHouse queries and bounded batch inserts.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  test: ^1.26.3
