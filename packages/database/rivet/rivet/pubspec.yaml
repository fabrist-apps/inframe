name: rivet
description: Generated, type-safe PostgreSQL access with owned connection lifetimes.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  chrono_id: ^0.1.0
  crypto: ^3.0.7
  meta: ^1.17.0
  postgres: 3.5.12

dev_dependencies:
  test: ^1.26.3
