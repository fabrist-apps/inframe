name: rivet
description: Generated, type-safe PostgreSQL reads with owned connection lifetimes.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  chrono_id: ^0.1.0
  postgres: 3.5.12

dev_dependencies:
  test: ^1.26.3
